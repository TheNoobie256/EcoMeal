{% extends 'base.html.twig' %}
{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }
    </style>
    <div class="example-wrapper">
        <p>{{ category.id }}</p>
        <p>{{ category.name }}</p>
        <a href="{{ path('app_category') }}">Back</a>
    </div>
{% endblock %}
